/* const mongoose = require('mongoose');
const transactionModel = require('../models/TransactionModel.js');

const db = {};
db.mongoose = mongoose;
db.url = process.env.DB_CONNECTION;
db.gradesModel = transactionModel(mongoose);

exports.db = { db };
 */
